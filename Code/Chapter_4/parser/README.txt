Files used for parsing the raw mbox files downloaded from the IETF archives.
To run, execute the “run.py” file.
The “settings.txt” file is used to specify various things the parser needs in order to run properly.
These need to be specified before execution.
